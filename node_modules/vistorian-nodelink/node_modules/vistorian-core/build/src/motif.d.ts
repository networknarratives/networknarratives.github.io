import { Link, Node, Time } from "./dynamicgraph";
export declare class Motif {
    nodes: Node[];
    links: Link[];
    times: Time[];
    constructor(nodes: Node[], links: Link[]);
    print(): void;
}
export declare class MotifTemplate {
    nodes: number[];
    links: number[][];
    constructor(nodes: number[], links: number[][]);
}
export declare class MotifSequence {
    motifs: Motif[];
    push(m: Motif): void;
}
