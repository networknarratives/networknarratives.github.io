import { DynamicGraph, IDCompound } from "./dynamicgraph";
export declare function searchForTerm(term: string, dgraph: DynamicGraph, type?: string): IDCompound;
export interface IFilter {
    test(o: any): boolean;
}
